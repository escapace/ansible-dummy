net.listen('{{ dummy_ipv4_cidr | ansible.utils.ipaddr('network') | ansible.utils.ipmath(1) }}', 53, { kind = 'dns', freebind = false })
net.listen('{{ dummy_ipv4_cidr | ansible.utils.ipaddr('network') | ansible.utils.ipmath(1) }}', 853, { kind = 'tls', freebind = false })
net.listen('{{ dummy_ipv6_cidr | ansible.utils.ipaddr('network') | ansible.utils.ipmath(1) }}', 53, { kind = 'dns', freebind = false })
net.listen('{{ dummy_ipv6_cidr | ansible.utils.ipaddr('network') | ansible.utils.ipmath(1) }}', 853, { kind = 'tls', freebind = false })
